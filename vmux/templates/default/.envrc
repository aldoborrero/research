use flake

# Load vmux environment: resolves secrets, exports VMUX_* metadata.
# Requires `source_file` of lib/use_vmux.sh in your direnvrc,
# or add to ~/.config/direnv/direnvrc:
#   source_file /path/to/vmux/lib/use_vmux.sh
use_vmux
